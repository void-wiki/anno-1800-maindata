{
  "private": true,
  "name": "@anno-1800-wiki/imgconv",
  "version": "6.2.0",
  "scripts": {
    "dotnet:build": "dotnet build -c Release",
    "dotnet:restore": "dotnet restore",
    "prepare": "yarn dotnet:restore && yarn dotnet:build"
  }
}
