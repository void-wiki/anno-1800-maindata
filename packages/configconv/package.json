{
  "private": true,
  "name": "@anno-1800-wiki/configconv",
  "version": "6.2.3",
  "scripts": {
    "clean:dist": "dotnet clean",
    "build": "dotnet build",
    "restore": "dotnet restore",
    "prepare": "yarn clean:dist && yarn restore && yarn build"
  }
}
